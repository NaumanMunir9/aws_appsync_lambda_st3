type Query {
  welcome: String
  hello: String
}

input ProductInput {
  name: String
  price: Int
}

type Product {
  name: String
  price: Int
}

type Mutation {
  addProduct(product: ProductInput): Product
}
